# GitHub Pages 部署配置说明

## 自动部署配置

本项目已配置GitHub Actions自动部署到GitHub Pages，包含以下功能：

### 🚀 自动化流程
- **代码推送**：推送到 `main` 或 `master` 分支时自动触发
- **测试验证**：运行单元测试、类型检查和代码规范检查
- **自动构建**：使用Vite构建生产版本
- **自动部署**：部署到GitHub Pages

### 📁 配置文件说明

#### 1. `.github/workflows/ci-cd.yml` (推荐使用)
完整的CI/CD流水线，包含：
- Node.js环境设置
- 依赖安装
- 代码检查(lint)
- 单元测试
- 类型检查
- 生产构建
- GitHub Pages部署

#### 2. `.github/workflows/deploy.yml`
简化的部署工作流，专注于构建和部署

#### 3. `vite.config.ts` 修改
```typescript
// GitHub Pages 配置
base: process.env.NODE_ENV === 'production' ? '/Project-Sekai_Role-test-vue/' : '/',
build: {
  outDir: 'dist',
  assetsDir: 'assets',
  rollupOptions: {
    output: {
      manualChunks: {
        vendor: ['vue', 'vue-router', 'pinia'],
        utils: ['html2canvas']
      }
    }
  }
}
```

## 🛠️ GitHub Pages 设置步骤

### 1. 启用GitHub Pages
1. 进入仓库的 **Settings** 选项卡
2. 点击左侧 **Pages** 菜单
3. 在 **Build and deployment** 部分：
   - Source 选择 **GitHub Actions**
   - 无需额外配置，Actions会自动处理

### 2. 配置仓库名称（重要）
确保你的仓库名称与 `vite.config.ts` 中的 `base` 路径匹配：

如果仓库名为 `Project-Sekai_Role-test-vue`，则配置正确：
```typescript
base: process.env.NODE_ENV === 'production' ? '/Project-Sekai_Role-test-vue/' : '/'
```

如果是其他名称，请相应修改。

### 3. 推送代码触发部署
```bash
git add .
git commit -m "Configure GitHub Pages deployment"
git push origin main
```

## 📊 部署状态监控

### 查看工作流状态
1. 进入仓库的 **Actions** 选项卡
2. 查看工作流运行状态
3. 点击具体运行查看详细日志

### 访问部署页面
部署成功后，应用可通过以下URL访问：
```
https://<your-username>.github.io/Project-Sekai_Role-test-vue/
```

## 🔧 故障排除

### 常见问题

1. **页面404错误**
   - 检查仓库名称是否与base路径匹配
   - 确认GitHub Pages已启用
   - 等待部署完成（通常需要1-2分钟）

2. **构建失败**
   - 查看Actions日志中的错误信息
   - 确保所有依赖正确安装
   - 检查TypeScript类型错误

3. **资源加载失败**
   - 确认 `vite.config.ts` 中的base配置正确
   - 检查构建输出的静态资源路径

### 调试命令
```bash
# 本地测试构建
npm run build

# 本地预览构建结果
npm run preview

# 检查构建输出
ls -la dist/
```

## 🎯 最佳实践

1. **分支策略**：使用 `main` 分支作为生产环境
2. **开发分支**：在 `develop` 或功能分支上开发，合并到 `main` 后自动部署
3. **版本控制**：定期打标签发布稳定版本
4. **监控**：关注Actions执行状态和Pages访问情况

## 📈 性能优化

构建配置已包含：
- **代码分割**：Vue、路由、状态管理库单独打包
- **资源压缩**：Vite自动压缩JS/CSS资源
- **缓存策略**：GitHub Pages提供CDN缓存

部署完成后，用户将获得快速、可靠的访问体验！